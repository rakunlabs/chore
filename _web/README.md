# Chore Frontend

Frontend communicate to backend with REST-API.

## Development

Run project, for HTTPS set HTTPS env value to non empty

```sh
pnpm run dev -- --host
```

Build

```sh
pnpm build
```
