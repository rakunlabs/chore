{
    "fields": {
       "project":
       {
          "key": "{{.project.key}}"
       },
       "summary": "{{.summary}}",
       "description": "{{.description}}",
       "issuetype": {
          "name": "{{.issuetype.name}}"
       }
   }
}